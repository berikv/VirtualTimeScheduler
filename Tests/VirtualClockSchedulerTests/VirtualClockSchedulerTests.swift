import XCTest
@testable import VirtualClockScheduler

final class VirtualClockSchedulerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(VirtualClockScheduler().text, "Hello, World!")
    }
}
